@model IEnumerable<Mantenimientos.Models.AsignaturaEstudiante>

@{
    ViewData["Title"] = "Index";
}

<h1>Registro</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asignatura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estudiantes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asignatura.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estudiantes.Apellido)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AsignaturaEstudianteID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AsignaturaEstudianteID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AsignaturaEstudianteID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
